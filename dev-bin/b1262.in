# S1
            @theme_args =
              get_themes(
                \%opt,
                ( $2 ? $2 :
                      (
                        shift(
                            @theme_args)
                          ||
                          shift(@ARGV)
                      ) ) );

# S2
            @theme_args =
              get_themes(
                \%opt,
                ( $2 ?
                      $2 : (
                        shift(
                            @theme_args)
                          ||
                          shift(@ARGV)
                      ) ) );
